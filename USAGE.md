# Руководство по использованию Ubuntu System Updater v2.0

## Установка

```bash
# Клонируйте репозиторий
git clone https://github.com/Fixcat/UbuntuAutoUpdater.git
cd UbuntuAutoUpdater

# Установите в систему
chmod +x install.sh
sudo ./install.sh
```

## Запуск

После установки запускайте программу командой:

```bash
sudo aupdate
```

## Полный список функций (36 опций)

### ОСНОВНОЕ (1-5)

#### 1. Сканировать и показать обновления
Проверяет наличие доступных обновлений и показывает список.

```bash
sudo aupdate
# Выберите: 1
```

#### 2. Обновить все компоненты
Обновляет все пакеты в системе.

```bash
sudo aupdate
# Выберите: 2
# Подтвердите: y
```

#### 3. Обновить конкретный пакет
Обновляет только указанный пакет.

```bash
sudo aupdate
# Выберите: 3
# Введите имя пакета
```

#### 4. Полное обновление (dist-upgrade)
Выполняет полное обновление с разрешением зависимостей.

```bash
sudo aupdate
# Выберите: 4
```

#### 5. Обновления безопасности
Устанавливает только критические обновления безопасности.

```bash
sudo aupdate
# Выберите: 5
```

## Удаление

Для удаления программы из системы:

```bash
cd UbuntuAutoUpdater
chmod +x uninstall.sh
sudo ./uninstall.sh
```

Или вручную:

```bash
sudo rm /usr/local/bin/aupdate
```

## Расширенные функции

### Управление пакетами (6-10)
- **6. Установить пакет** - установка нового пакета
- **7. Удалить пакет** - удаление пакета из системы
- **8. Переустановить пакет** - переустановка пакета
- **9. Поиск пакета** - поиск пакетов по имени
- **10. Информация о пакете** - детальная информация

### Обслуживание (11-15)
- **11. Автоочистка** - удаление ненужных пакетов и очистка кэша
- **12. Исправить зависимости** - исправление сломанных зависимостей
- **13. Проверить целостность** - проверка целостности пакетов
- **14. Удалить старые ядра** - очистка старых версий ядра
- **15. Показать размер кэша** - размер кэша APT

### Информация (16-20)
- **16. Информация о системе** - ОС, ядро, архитектура, uptime
- **17. Использование диска** - свободное место на диске
- **18. История обновлений** - последние 20 операций
- **19. Статистика обновлений** - общая статистика
- **20. Проверить перезагрузку** - нужна ли перезагрузка

### Дополнительно (21-25)
- **21. Показать установленные** - список установленных пакетов
- **22. Показать репозитории** - активные репозитории
- **23. Показать зависимости** - зависимости пакета
- **24. Показать файлы пакета** - файлы, установленные пакетом
- **25. Найти пакет по файлу** - какой пакет содержит файл

### Резервное копирование (26-27)
- **26. Создать резервную копию** - сохранить список пакетов
- **27. Восстановить из копии** - восстановить пакеты

### Расширенное (28-34)
- **28. Удержать пакет** - запретить обновление пакета
- **29. Снять удержание** - разрешить обновление
- **30. Показать удерживаемые** - список удерживаемых пакетов
- **31. Симуляция обновления** - показать что будет обновлено
- **32. Загрузить пакет** - скачать без установки
- **33. Показать changelog** - история изменений пакета
- **34. Тест скорости репозиториев** - проверка скорости

### Логи (35-36)
- **35. Экспорт логов** - сохранить логи в файл
- **36. Очистить логи** - удалить все логи

## Файлы и директории

Программа создает:
- `~/.config/aupdate/update.log` - лог операций
- `~/.config/aupdate/history.log` - история действий
- `~/.config/aupdate/backups/` - резервные копии

## Требования

- Ubuntu / Ubuntu Server
- Права root (sudo)
- apt package manager

## Устранение проблем

### Команда не найдена

Если после установки команда `aupdate` не работает:

```bash
# Проверьте установку
ls -la /usr/local/bin/aupdate

# Переустановите
cd UbuntuAutoUpdater
sudo ./install.sh
```

### Ошибка прав доступа

Всегда используйте `sudo`:

```bash
sudo aupdate
```

### Ошибка apt-get

Убедитесь, что вы используете Ubuntu или Debian-based систему.
